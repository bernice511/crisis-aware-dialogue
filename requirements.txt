pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.13
huggingface_hub>=0.20
torch>=2.2
transformers>=4.46,<6
datasets>=3.0
accelerate>=1.0
scikit-learn>=1.4
joblib>=1.3
peft>=0.13
streamlit>=1.38

# Not required to run the Streamlit demo or src/cradle_response/evaluate.py.
# Only needed for src/cradle_response/train_response_sft.py, which requires a
# CUDA GPU (guarded by a RuntimeError otherwise) and is unrelated to inference.
# trl>=0.15,<2
# bitsandbytes>=0.45
